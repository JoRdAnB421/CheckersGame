[project]
name = "checkersgame"
version = "0.1.0"
description = "Simple checkers game to be played by two people"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.3",
    "pygame>=2.6.1",
    "regex>=2026.2.28",
]
